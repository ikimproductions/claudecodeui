# explore: file tree root listing over 10,000 entries is cut silently
Goal: a brief for the fix "return { items, truncated } from the file-tree route and show a 'showing the first 10,000' line in the tree UI".
Questions (cite path:line): (1) where the 10,000 cap is applied in `server/modules/file-tree/file-tree.service.ts` and how the `truncated` flag is attached to directory nodes today; (2) the route in `server/modules/file-tree/file-tree.routes.ts` — its response shape and every client caller (`src/shared/api.ts`, `src/modules/file-tree/*`, hooks) that consumes it; (3) the client component that renders the tree body (`src/modules/file-tree/FileTree.tsx` or a FileTreeBody) and how a per-directory "truncated" node is rendered today, so the root case can reuse it; (4) existing tests for the service and the component (`server/modules/file-tree/tests/`, `src/modules/file-tree/**/*.test.*`), how they build a fixture directory; (5) gotchas: pagination/lazy loading of subdirectories, the WebSocket/file-watcher refresh path, i18n of UI strings.
Read-only. ≤ 600 words.
