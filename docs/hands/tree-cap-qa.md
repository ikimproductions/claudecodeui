# qa: file tree shows "Showing the first 10,000 entries…" when the root listing is cut
Platform: web (this is the claudecodeui fork, Vite + Express). Read AGENTS.md / README for how to run it; build with `VITE_IS_PLATFORM=true npm run build` if needed, start the server on a free port in the background with a temp DB/config (look at `server/index.ts` env: PORT, DATABASE paths) — never `open`, never a visible window.
Fixture: create a temp directory with 10,001 empty files and register it as a project (via the API the UI uses — read `src/shared/api.ts` for the projects create endpoint and auth: `VITE_IS_PLATFORM` mode may need a token, `server/modules/auth`).
Test with headless Playwright (python or `npx playwright` from node_modules if present; chromium headless, viewport 1280x800): open the project, open the Files tab, confirm (1) exactly 10,000 rows load and the status line "Showing the first 10000 entries" is visible above the list; (2) a project with 5 files shows NO such line; (3) the command palette file search and @-file mentions in chat still list files (the response shape changed to `{ items, truncated }`). Also hit `GET /api/file-tree/projects/<id>/files` directly and quote the JSON shape.
Report `docs/qa/tree-cap.md` (VERDICT first, numbered findings flow · expected · actual · repro · screenshot ≤ 600 px in docs/qa/shots/ · severity, then coverage). Never edit product code; stop what you started; delete the temp fixture.
