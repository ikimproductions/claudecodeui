# Spec — file tree: a root listing cut at 10,000 entries says so (2026-09-14)
From `todo` "Fork file tree: a root directory over 10,000 entries is cut silently". Brief: `docs/briefs/tree-cap.md`.
**Owner module:** `server/modules/file-tree` (service + route) and `src/modules/file-tree` (hook, body, reader helper). Consumers outside the module only unpack the new shape through the helper.
**Edge cases:** empty project (items [], truncated false); exactly 10,000 entries (not truncated); subtree request that is itself cut (same shape, banner not shown for subtrees — the directory row already shows its truncated state); stale/bare-array response from an older server (helper tolerates it); fetch aborted mid-flight (no state write); error response (banner hidden, error shown as today).
**Refactor first?** n — the service already computes `truncated`; the fix is to stop discarding it.
**Property tests:** n/a (no parser/state machine).
**Assumptions (decided from the brief's open questions):** (a) root and subtree requests both return `{ items, truncated }`; (b) all four consumers read it through `readFileTreeResponse()` in `src/modules/file-tree/utils/fileTreeResponse.ts` (accepts a bare array too); (c) the line renders in `FileTreeBody` above the list, English key `fileTree.truncatedRoot` (other locales fall back to en per `i18n/config.ts`).
## Tests first
- [x] T1 server: `listProjectFiles` returns `{ items, truncated }`; route test expects the object; a 10,001-entry root sets `truncated: true` [test] (class: mechanical, owns: server/modules/file-tree/**, server/shared/types.ts, deps: -)
- [x] T2 client helper: `readFileTreeResponse` unpacks `{items,truncated}`, tolerates a bare array, and never throws on garbage [test] (class: mechanical, owns: src/modules/file-tree/utils/fileTreeResponse.ts, deps: -)
- [x] T3 body: `FileTreeBody` renders "Showing the first 10,000 entries…" only when `truncated` [test] (class: judgment, owns: src/modules/file-tree/**, deps: T2)
- [x] T4 consumers: `useFileTreeData` (root + subtree), `useFileOpenResolver`, `useFilesSource`, `useFileMentions` read through the helper; `FileTree` passes `truncated` to the body; en i18n key (class: judgment, owns: src/modules/**, deps: T2)
## Parallel groups
- G1: T1 (hands implement) ‖ T2–T4 inline
